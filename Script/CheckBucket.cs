using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckBucket : MonoBehaviour
{
    /*private void OnCollisionEnter2D(Collision2D col)
    {
        if (col.gameObject.name == "Order1")
        {
            //Color myColor = GetComponent<Renderer>().material.color;
            Color otherColor = col.gameObject.GetComponent<SpriteRenderer>().color;
            Debug.Log("OtherColor"+otherColor);
            if (myColor.Equals(otherColor))
            {
                Destroy(col.gameObject);
            }
        }
    }*/
}
